<Window
  x:Class="Qt6uox4f1iujybm.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Qt6uox4f1iujybm"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="MainWindow"
  d:DataContext="{d:DesignInstance Type=local:ViewModel}"
  SizeToContent="WidthAndHeight"
  mc:Ignorable="d">
  <Window.Resources>
    <local:ImageConverter x:Key="ImageConverter" />
  </Window.Resources>

  <DockPanel>
    <ToolBar DockPanel.Dock="Top">
      <Button MinWidth="128" Click="OpenSelectDialogButton_Click" Content="背景画像の選択" />
      <Separator />
      <RadioButton
        MinWidth="32"
        MinHeight="32"
        Content="?"
        IsChecked="True" />
      <RadioButton MinWidth="32" MinHeight="32">
        <Rectangle Width="24" Height="16" Stroke="Black" />
      </RadioButton>
    </ToolBar>
    <Grid>

      <!--
        ロックさせないコンバータ
        [WPFで画像表示時にファイルをロックしないようにしたい - かずきのBlog@hatena](https://blog.okazuki.jp/entry/2015/06/20/122427)
      -->
      <Image
        Width="1000"
        Height="800"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Source="{Binding SelectedPictItem.FilePath, Converter={StaticResource ImageConverter}}"
        StretchDirection="DownOnly" />
    </Grid>
  </DockPanel>
</Window>
